{% extends 'base.html.twig' %}

{% block title %}Modification Recruteur
{% endblock %}

{% block body %}
	<div class="mt-4 container">
		<h1>Modification Recruteur</h1>

		{{ include('pages/user/_form.html.twig', {'button_label': 'Modifier'}) }}

		<a href="{{ path('home.index') }}" class="btn btn-secondary btn-lg mt-4">Retour accueil</a>


		{{ include('pages/user/_delete_form.html.twig') }}</div>

{% endblock %}
