{% extends 'base.html.twig' %}

{% block title %}Description de l'annonce
{% endblock %}

{% block body %}
	<div class="container mt-4">
		<h1>Description de l'annonce</h1>


		<table class="table">
			<tbody>
				<tr>
					<th>Réf.</th>
					<td>{{ job.id }}</td>
				</tr>
				<tr>
					<th>Titre</th>
					<td>{{ job.title }}</td>
				</tr>
				<tr>
					<th>Lieu</th>
					<td>{{ job.workPlace }}</td>
				</tr>
				<tr>
					<th>Description</th>
					<td>{{ job.description }}</td>
				</tr>
				{# <tr>
																													<th>IsVisible</th>
																													<td>{{ job.isVisible ? 'Yes' : 'No' }}</td>
																												</tr> #}
			</tbody>
		</table>

		<a href="{{ path('app_job_index') }}">Retour à la liste</a>

		<a class="btn btn-danger btn-lg mt-4" href="{{ path('app_candidature_new') }}">Je veux postuler TODO</a>


		{# <a href="{{ path('app_job_edit', {'id': job.id}) }}">edit</a>
										
												{{ include('pages/job/_delete_form.html.twig') }}</div> #}

	{% endblock %}
