{% extends 'base.html.twig' %}

{% block title %}Validation du  profil
{% endblock %}


{% block body %}
	<div class="container mt-4">
		<h1>Completer votre profil-Candidat</h1>

		{# {{ include('pages/candidate/_form.html.twig', {'button_label': 'Update'}) }} #}

		{{ form_start(form) }}

		{# <div class="form-group d-none">
															{{ form_label(form.cvName) }}
															{{ form_widget(form.cvName) }}
															<div class="form-error">
																{{ form_errors(form.cvName) }}
															</div>
														</div> #}


		{{ form_widget(form) }}
		<button class="btn btn-secondary btn-lg mt-4">{{ button_label|default('Valider') }}</button>

		{{ form_end(form) }}


		<a class="btn btn-secondary btn-lg mt-4" href="{{ path('home.index') }}">Retour à l'accueil</a>


		{{ include('pages/candidate/_delete_form.html.twig') }}</div>

{% endblock %}
