<form method="post" action="{{ path('app_recruiter_delete', {'id': recruiter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recruiter.id) }}">
	<button class="btn btn-danger btn-lg mt-4">Supprimer</button>

</form>
